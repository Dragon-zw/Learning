apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc
spec:
  storageClassName: longhorn-single-replica # longhorn
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi